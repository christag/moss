/**
 * Setup Layout
 * Excludes navigation and provides clean setup wizard experience
 */

export default function SetupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
